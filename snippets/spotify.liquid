<script src="{{ 'spotify-embed.v1.js' | asset_url }}" async></script>

<div id="spotify-iframe" class="bg-slate-100 h-60 rounded-lg animate-pulse"></div>

<script>
    window.onSpotifyIframeApiReady = (IFrameAPI) => {
    const element = document.getElementById('spotify-iframe');
    const options = {
        height: '200',
        uri: 'spotify:album:668sak5iUcXZeaOwq3fERM'
        };
    const callback = (EmbedController) => {};
    IFrameAPI.createController(element, options, callback);
    };
</script>